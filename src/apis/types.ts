export type Category={
    createdAt:string,
    name: string,
    id: string|number
}

export type Product={
    createdAt:number
    name: string,
    avatar: string,
    developerEmail: string,
    price: string|number,
    id: string|number,
    description: string|number,
    category: string
}

